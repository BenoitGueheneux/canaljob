{% extends 'base.html.twig' %}

{% block title %}
	{{ controller_name }}
{% endblock %}

{% block body %}
	<div class="example-wrapper">
		<h1>{{ controller_name }}</h1>
		<ul>
			{% for user in users %}
				<li>
					{{ user.firstName }} {{ user.lastName }} {{ user.email }}
                    <a><a href="{{ path('user_space_collaborator_add', {id: user.id}) }}">ajouter</a></a>
				</li>

			{% endfor %}
		</ul>
	</div>
{% endblock %}
